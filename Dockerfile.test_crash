FROM python:3.12-slim

RUN pip install --no-cache-dir fastapi uvicorn

WORKDIR /app

COPY tests/crash_app.py /app/crash_app.py

ENV CRASH_AFTER_S=300
EXPOSE 80

CMD ["uvicorn", "crash_app:app", "--host", "0.0.0.0", "--port", "80"]
